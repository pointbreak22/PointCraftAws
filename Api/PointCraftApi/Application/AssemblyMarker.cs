namespace Application;

/// <summary>Used as `typeof(AssemblyMarker).Assembly` to point MediatR/DI at this assembly without depending on any specific handler existing.</summary>
public sealed class AssemblyMarker;
